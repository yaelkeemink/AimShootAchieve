@model ReisViewModel

<a href="/PublicReis/Details/@Model.Id">
    <div class="flexReisContainer">
        @Html.HiddenFor(a => Model.Id)
        <p class="country"> @Html.DisplayFor(a => Model.Naam)</p>
        <br />
        <meter value=@Model.ResiStatusInt min="0" max="3" high="3" low="1" optimum="3"></meter>
    </div>
</a>